import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'ionic.fruitlicious',
  appName: 'fruitlicious',
  webDir: 'dist',
  backgroundColor: '#e1f0d4',
  ios: {
    backgroundColor: '#e1f0d4',
  },
  android: {
    backgroundColor: '#e1f0d4',
  },
}

export default config
